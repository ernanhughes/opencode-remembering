# Bundled remembering engine (opencode-remembering product code).
"""Decisive evidence + provenance: admission is permission, selection
is necessity. Deterministic, reason-coded, budget-explicit."""

from remembering import ENGINE_VERSION  # noqa: F401
